# Phase 09 — Evals + Quality Gates Layer

## Added in this phase

- `packages/evals/`
- eval suite specification
- quality gate specification
- routing eval cases
- domain eval cases
- golden examples
- scoring rubrics
- eval reports
- quality gate reports
- CI-friendly scripts

## New commands

```bash
pnpm evals:list
pnpm evals:route "build a trading journal"
pnpm evals:run
pnpm gates:run
pnpm evals:report
```

## What this phase unlocks

AI Council can now measure itself. That is the difference between a large prompt library and a system that can improve over time.
