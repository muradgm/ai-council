export interface ProjectAIConfig { projectId: string; displayName: string; defaultCouncil: string; allowedAgents: string[]; allowedLanguages: string[]; tools: Record<string, string>; memory: { scope: "project" | "session"; allowLongTermMemory: boolean; allowUserMemory: boolean; redactSecrets: boolean; }; safety: { requireHumanApprovalFor: string[]; }; evaluation: { requiredBeforeRelease: string[]; }; }
