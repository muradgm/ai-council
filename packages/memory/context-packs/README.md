# Context Packs

A context pack is a compact working brief assembled from project memory, decision history, session notes, skills, agents, engines, workflows, and templates.

Context packs are stored in `storage/context-packs/<project>/` so Codex can load a single file at session start.
